<div class="list_question">
  <h4> <%= question.question %> </h4>
  <% question.answers.each do |answer|  %>
    <div>
      <input type="radio" name="question-<%= question.id %>" value="<%= answer.content %>">
        <%= answer.content %>
      </input>
    </div>
  <% end %>
</div>
